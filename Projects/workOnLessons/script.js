//create toggle button (when clicking on it - add class 'toggle', when cliking again - remove class)
function makeFiltersCounties(){
    const filterNode = document.querySelector('#filters');
    filterNode.addEventListener('click', function(event)
    {
        const taget = event.target;
        const filterValue = target.getAttribute();
    
    }
    makeFiltersCounties();
